# Codex Input Enhancer 开发约定

这是一款 Windows 本地优先的 Tauri 应用。目标是在不破坏用户原有输入的前提下，帮助用户生成更完整、可执行的 Codex 任务提示词。

## 先理解，再修改

- 先阅读与任务直接相关的现有代码、设置和日志；不要凭局部现象重写无关模块。
- 当 API 用法、版本差异、权限边界或系统行为不确定时，先查当前依赖的官方文档：例如 Tauri、React、Ant Design、Rust crate 或 DeepSeek API 的文档。
- 对复杂的新能力（系统集成、快捷键、剪贴板、文件权限、复杂交互）先寻找成熟开源实现作为参考。仅在许可证兼容、依赖成本合理且适配收益明确时复用；简单功能不需要为了“复用”而引入依赖。
- 若调研或复用影响了方案，交付时用一两句话说明参考来源与取舍即可。

## 代码原则

- 修改保持小而聚焦：不把无关重构、改名或样式整理混入功能修改。
- 使用清晰的领域命名；避免缩写、魔法字符串和难以理解的单行复杂逻辑。
- 一个模块或组件应有一个明确职责。发现两个独立职责、重复业务规则或跨层调用时，优先提取小函数、组件或适配层。
- 保持高内聚、低耦合：UI 不直接实现系统能力；Tauri 调用集中在 `src/lib/`；设置、日志、快捷键和剪贴板逻辑各自保持单一归属。
- 优先复用项目已有模式与组件；新增抽象必须减少实际重复或隔离明确的变化点。
- 所有用户可见文案使用中文，并处理加载、失败、取消和保存失败等主要状态。

## 安全与数据边界

- 不记录或输出 API Key、草稿正文、资料正文、完整本地路径等敏感内容。
- 选区是唯一允许写回 Codex 的目标；失败、取消或焦点异常时不得修改用户输入。
- 涉及剪贴板、凭据、资料目录、网络、全局快捷键或窗口焦点的改动，必须同时考虑成功和失败路径。
- 不扩大文件、网络、进程或模型工具权限，除非任务明确要求且实现了对应校验。

## 质量与验证

- 纯逻辑、设置校验、转换规则和可稳定复现的 Bug，优先新增或更新单元测试。
- 不要求为每个薄 UI 或 Windows 系统交互强行补自动化测试；这类改动应运行相关检查，并在可行时做一次应用内验证。
- 前端改动至少运行 `npm run check:frontend`；Rust 改动至少运行 `npm run check:backend`；跨端改动运行 `npm run check`。
- 改动桌面端行为后，重新构建并启动应用验证；如无法验证，要明确说明原因和风险。

## 交付说明

最终回复简要说明：改了什么、是否有调研/复用取舍、运行了哪些检查，以及仍未验证的风险。不要把“能编译”视为唯一完成标准。
