# Codex Input Enhancer 设计规范 v1.0

状态：提案，作为后续 UI 重构、组件验收和设计评审的唯一基线。
适用范围：预览/替换浮窗、澄清步骤、完整对比、控制面板、设置表单、目录管理、诊断、Toast 与空状态。
不适用范围：Codex 主窗口、网页内容、模型生成文本本身。

## 1. 产品界面角色

Codex Input Enhancer 是一个 Windows 桌面上的“短暂决策工具”，不是网页管理后台，也不是聊天应用。它在用户已有工作流中出现，帮助完成一件小而明确的事：选择上下文、补足必要信息、审阅替换文本、确认或取消。

因此所有界面应符合以下原则：

1. **安静**：一次只让用户判断一件事。没有装饰性卡片、渐变、炫光或抢注意力的色块。
2. **可预期**：同一类操作始终使用同一组件、位置、文案和快捷键；没有“同一功能多种入口/样式”。
3. **可逆**：确认替换是唯一主操作；取消、关闭和 Esc 永远不改写选区。危险删除必须二次确认。
4. **就近完成**：浮窗只承载本次增强会话；长期配置、导入和诊断只在控制面板中完成。
5. **桌面原生感**：窗口有清晰标题栏和拖拽区，控件密度适中，鼠标与键盘等价；页面不模拟移动端卡片流。
6. **可读而不喧闹**：信息层级依靠字号、字重、间距和有限的色彩语义，而非大面积边框与阴影。

本规范借鉴《易微联 APP 6.0 设计规范》中“高效、简约、通透”、令牌化色彩、统一文字层级、组件状态与反馈分层的方法；视觉语言改为适合本项目的北欧浅暖中性色与低饱和松绿，不复用其移动端尺寸和品牌蓝。

## 2. 信息架构与窗口职责

| 窗口/层级 | 只负责什么 | 不应出现什么 |
| --- | --- | --- |
| 会话浮窗 | 本次上下文、必要澄清、预览、替换确认 | 长期设置、目录导入、诊断列表、复杂滚动表单 |
| 完整对比 | 在不离开会话的前提下查看/编辑全文 | 额外配置、嵌套弹窗、无关状态 |
| 控制面板 | 服务、Agent、知识库、组合、功能开关、快捷键、诊断 | 本次选区正文、替换确认 |
| Toast | 已完成且无需选择的短反馈 | 错误详情、操作按钮、长说明 |
| 对话框 | 不可逆或阻断型决策 | 多步流程、设置编辑、复杂选择 |

### 2.1 窗口规格

| 场景 | 建议尺寸 | 规则 |
| --- | --- | --- |
| 会话浮窗 | 560 x 430 px（当前基线） | 固定尺寸、居中打开、无外层滚动条、无系统标题栏、始终可拖动 |
| 完整对比 | 680 x 500 px 模态层 | 只允许正文区域内部滚动；关闭后返回预览，不丢失编辑内容 |
| 控制面板 | 760 x 620 px | 固定尺寸；仅内容区滚动，标题栏、提示栏、页签和底部提交区固定 |
| Toast | 300 x 50 px | 屏幕水平居中、距屏幕顶部约 25%，不抢焦点，2 秒后淡出 |

所有应用窗口均不可调整大小，窗口边缘不显示 resize cursor。窗口打开后焦点只在需要立即输入时进入控件；其余情况下不抢占 Codex 输入。点击会话浮窗外部或 Esc 关闭会话浮窗；控制面板不因点击外部关闭。

固定尺寸不代表允许内容溢出：长内容必须截断、折行、放入唯一的内部滚动区，或进入“完整对比”等专用视图；不能依赖用户拉大窗口来补救布局。

### 2.2 会话流程

```text
上下文 → （仅必要时）澄清 → 生成中 → 预览/编辑 → 替换 或 取消
```

每个阶段只能显示一个主任务。不要把“选择 Agent/知识库”“问题”“全文编辑”“诊断”同时堆在一屏。

- 上下文：显示当前草稿摘要、工作组合、可选参考上下文；主操作为“继续”。
- 澄清：每屏一个问题；选项与自由输入并存；没有关键缺口时跳过。
- 生成中：只显示可理解的当前状态，例如“正在理解任务”“正在结合知识库”“正在生成建议”；不显示原始工具调用或协议文本。
- 预览：原文与建议并列；唯一主按钮为“替换”。“重新生成”“完整对比”均为次级操作。

## 3. 基础令牌（Tokens）

所有颜色、字号、间距、圆角、阴影和动画必须通过令牌引用。业务组件不得重新发明近似颜色，例如 `#78877d`、`#78967d`、`#6e8e76` 混用来表达同一状态。

### 3.1 色彩

默认只提供浅色主题。不得使用纯黑背景搭配黑色文字；文本与底色必须使用明确成对令牌。

| 分类 | 令牌 | 值 | 用途 |
| --- | --- | --- | --- |
| 页面底 | `--color-canvas` | `#F4F5F1` | 窗口与固定底栏底色 |
| 次级底 | `--color-subtle` | `#EEF0EB` | 状态条、选中前的轻背景 |
| 容器 | `--color-surface` | `#FCFCF9` | 表单、预览块、弹出层 |
| 浮层 | `--color-elevated` | `#FFFFFF` | 对话框、下拉层 |
| 边框 | `--color-border` | `#D9DED6` | 默认分隔与输入框 |
| 悬停边框 | `--color-border-strong` | `#BBC5B9` | 可交互组件 hover/focus 前态 |
| 主文字 | `--color-text` | `#29352E` | 标题、正文、图标 |
| 次文字 | `--color-text-muted` | `#647168` | 说明、元数据 |
| 弱文字 | `--color-text-subtle` | `#8B958D` | 占位、时间、辅助提示 |
| 主色 | `--color-primary` | `#6F9078` | 唯一主操作、选中、焦点 |
| 主色 hover | `--color-primary-hover` | `#5F7D68` | 主操作 hover |
| 主色弱化 | `--color-primary-subtle` | `#E8F0E8` | 选中底色、成功背景 |
| 信息 | `--color-info` | `#6E91A5` | 中性提示与链接，不与主操作竞争 |
| 成功 | `--color-success` | `#5D8A68` | 成功状态 |
| 警告 | `--color-warning` | `#B4813F` | 需要注意但可继续 |
| 危险 | `--color-danger` | `#B65E55` | 删除、失败、不可逆操作 |
| 危险弱化 | `--color-danger-subtle` | `#F8EAE7` | 错误容器底色 |
| 遮罩 | `--color-scrim` | `rgba(32, 42, 36, .28)` | 完整对比与确认弹层 |

规则：主色只表示“当前选择/唯一主操作/键盘焦点”，不用于所有图标和文字。成功、警告、危险各自只承担状态语义，不能当装饰色。

### 3.2 字体

字体栈：`"Microsoft YaHei UI", "Segoe UI Variable", "Segoe UI", system-ui, sans-serif`；代码/快捷键/原文使用 `"Cascadia Mono", Consolas, monospace`。

| 令牌 | 字号/行高/字重 | 使用场景 |
| --- | --- | --- |
| `--text-micro` | 10 / 14 / 400 | 时间、极短状态，不用于核心操作 |
| `--text-caption` | 12 / 18 / 400 | 说明、标签、快捷键提示 |
| `--text-body` | 13 / 20 / 400 | 表单、列表、按钮、正文 |
| `--text-body-medium` | 13 / 20 / 500 | 选项、列表标题、强调正文 |
| `--text-section` | 14 / 20 / 600 | 分区标题、预览标签 |
| `--text-heading` | 16 / 24 / 600 | 浮窗步骤标题、控制面板标题 |
| `--text-display` | 20 / 28 / 600 | 仅用于空状态或极少数关键标题 |

正文、设置说明、列表和表单标签一律左对齐；仅短 Toast、空状态和确认对话框可居中。单行列表标题超出时省略；说明最多两行；正文预览使用截断与“查看完整对比”，不允许撑破边界。

### 3.3 间距、圆角、边框与阴影

| 分类 | 令牌 | 值 |
| --- | --- | --- |
| 间距 | `--space-1/2/3/4/5/6` | 4 / 8 / 12 / 16 / 20 / 24 px |
| 圆角 | `--radius-control` | 4 px |
| 圆角 | `--radius-surface` | 6 px |
| 圆角 | `--radius-dialog` | 8 px |
| 边框 | `--border-default` | 1 px solid `--color-border` |
| 轻浮层阴影 | `--shadow-float` | `0 8px 24px rgba(32,42,36,.10)` |
| 下拉/Popover 阴影 | `--shadow-menu` | `0 4px 12px rgba(32,42,36,.10)` |

组件内部使用 8 或 12 px，分区之间使用 16 或 20 px；禁止随意出现 7、9、10、14 px 的局部间距。常规容器靠底色和细边框分层，不使用可见的大阴影。

### 3.4 动效与焦点

- hover、颜色、边框变化：120 ms ease-out。
- 弹层出现：160 ms ease-out；关闭：120 ms ease-in。
- Toast：淡入 150 ms，停留 2 秒，淡出 300 ms。
- 不使用弹跳、长缓动、强缩放、持续闪烁或加载时反复改变窗口尺寸。
- 键盘焦点：`0 0 0 2px rgba(111,144,120,.28)`，不得只依赖颜色变化。

## 4. 通用组件规范

### 4.1 桌面标题栏

- 高度 36 px；左侧 12 px、右侧 8 px 内边距；底部一条边框。
- 整个标题文本区可拖动；按钮、输入框、下拉框、复选框及其父级必须标记为不可拖动。
- 左侧只显示当前窗口名称；右侧仅放与窗口生命周期有关的动作：返回、设置、关闭。
- 关闭按钮点击热区至少 32 x 32 px；图标 16 px；不使用大号圆形按钮。

### 4.2 按钮

| 等级 | 视觉 | 用途 | 每个操作区上限 |
| --- | --- | --- | --- |
| 主按钮 | 主色实底、白字 | 提交、继续、替换、保存 | 1 个 |
| 次按钮 | 表面底、细边框 | 取消、重新生成、完整对比 | 2 个可见，更多收进菜单 |
| 文字按钮 | 无边框 | 返回、测试、轻编辑 | 不承载危险确认 |
| 危险按钮 | 危险文字或弱危险底 | 删除、清空日志 | 仅在确认流程中使用 |
| 图标按钮 | 32 x 32 热区 | 关闭、编辑、删除 | 必须有 `aria-label`/Tooltip |

常规高度：紧凑 28 px，标准 32 px，底部主操作 36 px。按钮文案使用动词，例如“继续”“替换”“保存修改”；不要使用“确定”承担不明确的含义。

### 4.3 输入、选择和快捷键录入

- 标准控件高度 32 px，圆角 4 px，左右内边距 8 px。
- 每个表单项由“标签 + 控件 + 可选的单行说明/错误”构成；错误紧贴对应控件，不用 Toast 代替字段错误。
- 标签使用 `--text-caption` / 500；说明使用 `--text-micro`；密码与 API Key 默认遮蔽。
- 聚焦只改变边框和焦点环；不会滚动整个窗口。
- 快捷键录入框使用等宽字体，并在录入期间显示明确状态“正在录入，按 Esc 取消”。
- Select 仅用于预设项；输入框不伪装成下拉菜单。

### 4.4 列表、单元格和目录项

- 单元格最小高度 48 px；标题一行，描述最多两行；项目之间用细分隔线，不再套额外卡片。
- 左侧为名称和摘要，中间可显示单一状态，右侧为最多 2 个可见动作与一个“更多”入口。
- 默认项应使用“默认”文字标签或单选样式，不能只靠一个没有说明的勾选框。
- Agent、知识库、AI 服务和组合的列表都复用同一 `ResourceListItem` 结构，避免各自定义边距、按钮和空状态。

### 4.5 复选、多选、Radio 与 Switch

- Checkbox：仅表示可并存、可临时启用的内容，例如“本次附带参考上下文”。
- Radio：表示互斥选择，例如 API 协议、参考上下文分类。
- Switch：只用于立即生效且可逆的长期功能开关，例如联网能力；切换后必须有简短状态反馈。
- 不得用 Checkbox 表示“设为默认”；应使用 Radio 或明确的“设为默认”文字操作。

### 4.6 对话框、Popover、Toast

- 对话框只用于确认删除、不可逆行为和阻断错误；正文不超过 3 行，最多一个主操作和一个取消操作。
- “完整对比”属于任务层查看，使用独立模态而非确认对话框；其编辑区可滚动，但浮窗主体不滚动。
- Popover 用于 3 至 6 项轻量菜单；复杂设置一律进入控制面板页面。
- Toast 仅报告“已保存”“参考已保存”“已复制”等无需选择的结果；成功 2 秒、警告 4 秒、错误不自动消失或提供“查看详情”。

### 4.7 空、加载、错误和禁用状态

| 状态 | 表现 | 用户下一步 |
| --- | --- | --- |
| 初始/空 | 简短图标、标题、解释、一个相关动作 | 例如“导入 Agent” |
| 加载 | 固定布局内的状态文字与小型 Spin/Skeleton | 等待或取消；不显示协议细节 |
| 字段错误 | 控件边框、字段下方具体原因 | 直接修正输入 |
| 请求错误 | 状态栏/Notice 展示可行动原因 | 重试、检查设置或打开诊断 |
| 禁用 | 低对比但可读，不可点击 | 与说明相邻呈现禁用原因 |
| 成功 | 一次性 Toast 或状态行 | 自动回到可操作状态 |

禁止把 API Key、选区正文、参考上下文、完整路径或原始 API 响应放进错误、日志预览或 Toast。

## 5. 页面规范

### 5.1 会话浮窗

标题栏下方仅保留一个 36 px 状态条。状态条包含状态图标和一句短文本，不作为第二个标题区。

**上下文步骤**

- 当前草稿以一行代码字体摘要展示，最多 120 字；超出省略。
- 参考上下文是可选的内联信息条，不是卡片组；内容摘要、启用 Checkbox、移除按钮同一行。
- “工作组合”是主要选择；选择“通用增强”时应显示一句“本次不使用 Agent 与知识库”，避免空白含义。
- 底部固定操作区：`取消` + `继续`。

**澄清步骤**

- 顶部显示“需要补充信息 · 1/2”，然后是一句问题。
- 一题一屏；最多 3 个问题；选项不超过 5 项，每项最多两行。
- 始终提供自由输入框，placeholder 为“或直接补充你的答案”。
- 仅在回答会实质改变下游任务时提问；不得询问工具自身是否应该“直接回答、给建议或写提示词”。

**预览步骤**

- 原文与建议并列，两列等宽，容器最小宽度为 0，内部文本必须 `overflow-wrap:anywhere`，不得溢出边框。
- 每列展示最多 8 行；长内容只能通过“查看完整对比”阅读。
- 按钮从次到主排列：`取消`、`完整对比`、`重新生成`、`替换`。其中“替换”是唯一实底按钮。
- 键盘：Enter 替换、Esc 取消、Tab 在关键操作与完整对比之间移动；不要覆盖文本编辑区的正常 Tab 行为。

### 5.2 控制面板

- 标题栏：`控制面板`；从会话进入时才显示“返回优化”，独立打开时不显示返回。
- 提示栏：只显示当前保存/测试/导入结果；成功信息不长期占据该区域。
- 页签顺序：工作组合、AI 服务、Agent、知识库、功能与快捷键、诊断。先放“日常设置”，把诊断置后。
- 每个页签使用相同的“分区标题 + 说明 + 内容 + 固定操作区”骨架。
- 编辑服务或工作组合时使用页内二级页面，不在页签内容里嵌套弹窗；返回后保留列表滚动位置。

### 5.3 AI 服务页

- 顶部只有“AI 服务”标题、说明和“添加服务”。
- 列表项显示服务名、协议、模型、默认状态；动作收敛为“测试”“编辑”“更多”。
- “测试”期间按钮显示加载，并在提示栏输出明确结果；不是“没有文本内容”这类内部适配错误。
- 编辑页按以下顺序：服务名称、协议、服务地址、模型名称、API Key、保存。协议说明中写明“服务响应形式由应用自动适配”，不要求用户理解流式/非流式。

### 5.4 Agent、知识库、组合

- 导入、删除、生成索引等属于资源操作，列表统一用资源单元格。
- 删除确认必须说明影响范围，例如“只移除本工具中的已导入记录，不删除原目录”或反之；文案必须与实际行为一致。
- 知识库索引状态使用文本 + 语义色：未索引/可用/需要更新/失败；不单靠图标。
- 工作组合是“本次上下文预设”，编辑页应清楚显示 Agent 单选和知识库多选的差异。

### 5.5 功能与快捷键、诊断

- 功能开关与快捷键分成两个分区；参考上下文读取方式属于参考上下文分区，不与全局快捷键混排。
- 保存区固定在底部，只在存在未保存修改时高亮或提示。
- 诊断默认展示事件摘要，复制/清空采用次级或危险操作；不得将敏感内容渲染为列表详情。

## 6. 内容与文案规则

- 中文使用全角标点；操作文案使用动词开头；同一概念保持同一名称，例如始终使用“工作组合”，不混用“组合/预设”。
- 状态必须描述“发生了什么 + 可以做什么”：`AI 服务连接失败，请检查服务地址、协议或 API Key。`
- 不向用户暴露 Rust、Tauri、SSE、DSML、原始 JSON、HTTP 库或内部命令名。
- 不用“操作成功完成（0x00000000）”这类系统文案。系统错误应被映射为可理解提示，诊断中才保留安全的错误分类与时间。
- 生成状态采用进行式：`正在读取选区`、`正在理解任务`、`正在生成建议`、`正在准备替换`。

## 7. 无障碍与键盘

- 所有图标按钮必须有可访问名称；颜色不是唯一状态信号。
- 文本与背景对比度：普通正文至少 4.5:1；大文本至少 3:1；禁用文本仍需可辨识。
- 每个浮窗必须存在可见焦点顺序，Esc 永远关闭当前可关闭层级；Enter 只触发当前唯一主操作。
- 关闭、取消、返回、替换、保存和删除的快捷键/焦点行为在所有页面一致。
- 文本截断处应可通过完整对比、Tooltip 或可聚焦的展开操作获取完整内容。

## 8. 实施映射与验收

### 8.1 建议的代码落点

| 目标 | 建议落点 |
| --- | --- |
| 全局令牌与 Ant Design token | `src/styles/foundation.css`、`src/main.jsx` |
| 标题栏、按钮栏、预览块 | `src/components/` |
| 会话流程与状态条 | `src/features/palette/` |
| 资源列表、表单、页签、固定操作区 | `src/features/control/` |
| 窗口尺寸、可拖拽、焦点与可见性 | `src-tauri/tauri.conf.json` 与窗口管理代码 |

不要以一次大改替换全部 CSS。顺序应为：先建立令牌和通用组件，再统一会话浮窗，然后控制面板，最后做状态/空态/错误的视觉回归。

### 8.2 视觉验收清单

- 同类主按钮、输入框、列表项、状态条、弹层是否共享同一高度、圆角、边框和焦点样式。
- 会话浮窗、控制面板、完整对比是否各自只有一个可滚动内容区域；窗口本身没有滚动条。
- 任意长中文、英文 URL、日志片段、代码和快捷键是否不会溢出容器。
- 是否存在纯黑底 + 黑字、低对比禁用态、无标签图标按钮或同时出现多个主按钮。
- 删除、替换、关闭、Esc、点击外部、API 测试失败、无选区、无目录、无列表等路径是否都有一致且安全的反馈。
- 100% 与 125% Windows 缩放下是否无裁切、重叠、失焦或窗口跳动。
- 键盘完成上下文选择、澄清、完整对比、替换和取消是否不依赖鼠标。

## 9. 当前差距

现有代码已经具备浅色中性基底、主色克制、固定标题栏、预览截断和部分无滚动外壳；但令牌仍分散，绿色近似值较多，控制面板的页签密度与资源操作不统一，部分列表/表单/状态区各自定义样式，尚未形成可验证的组件系统。

后续 UI 改造应以本规范为准，不再按单个页面临时加颜色、间距或“看起来差不多”的组件样式。
